import React, { useState, useEffect } from "react"
import { Box, Heading } from "rebass"
import { Button } from "../elements"

export const SliderButton = ({ next, back }) => {
  return <Button>Hi</Button>
}
